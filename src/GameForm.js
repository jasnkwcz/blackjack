import React, { Component } from 'react';

class GameForm extends Component {
	render() {
		return (
			<form>
				<button>Hit</button>
				<button>Stay</button>
			</form>
		);
	}
}

export default GameForm;
